import { IMessage } from "src/app/_models/admin/message";

export class messageWithError
{
    cvRefIdsInserted: number[]=[];
    //messages: IMessage[]=[];
    errortring: string='';
    notification: string='';
}
